function threeSum(arr, target) {
     let binaryNum = new Array(32);
 
   
    let i = 0;
    while (n > 0) {
 
      
        binaryNum[i] = n % 2;
        n = Math.floor(n / 2);
        i++;
    }
  binaryNum.reverse();
	return binaryNum;
    
}

module.exports = threeSum;
